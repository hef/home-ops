---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: foundry-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  #storageClassName: longhorn-static
  claimRef:
    name: foundry-data
  resources:
    requests:
      storage: 1Gi
