apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hass-config
  namespace: default
spec:
  storageClassName: longhorn-static
  claimRef:
    name: hass-config
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
